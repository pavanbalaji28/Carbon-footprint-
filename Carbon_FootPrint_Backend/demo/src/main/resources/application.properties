# Database Configuration
# Added &allowPublicKeyRetrieval=true at the end
spring.datasource.url=jdbc:mysql://localhost:3306/carbon_footprint_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
























# Hibernate Configuration (JPA)
# 'update' validates the schema and updates it if necessary. 
# Since we created tables manually, this ensures Java matches MySQL.